import { InputCardDirective } from './input-card.directive';

describe('InputCardDirective', () => {
  it('should create an instance', () => {
    const directive = new InputCardDirective();
    expect(directive).toBeTruthy();
  });
});
