import { InputCardFechaDirective } from './input-card-fecha.directive';

describe('InputCardFechaDirective', () => {
  it('should create an instance', () => {
    const directive = new InputCardFechaDirective();
    expect(directive).toBeTruthy();
  });
});
